# ======================================================================== #
# A ajouter dans ~/.bashrc 

#######################
# Sourcing Env
#
# Préparation au mode distant pour le déploiement
# rapide des differents modes d'utilisation
# 
# (J-1)
# 13/02/12 - Kinani Nawfel
#######################

# On check l'existance du lien symbolique qui sera modifié par la suite & on verifie que l'on est pas en pleine install
login=$(whoami)
if [ -f ~/.${login}.env.sh ]; then 
	if [ -z ${DEVTK_REQUIRE_CLEAN_INSTALL_ENVIRONMENT+x} ]; then
		. ~/.${login}.env.sh
	fi 
fi

# ======================================================================== #
